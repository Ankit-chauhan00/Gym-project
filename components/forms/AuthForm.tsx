import React from 'react'
import { Form } from '../ui/form';

interface Props {
    schema: string;
    formType: string;
    defaultValues: string;
    onSubmit: string;
}


const AuthForm = ({schema, formType, defaultValues, onSubmit}: Props) => {
  return (
    <>
    </>
  )
}

export default AuthForm